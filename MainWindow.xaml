<Window x:Class="SubtitleVideoPlayerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SubtitleVideoPlayerWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <MediaElement x:Name="videoElement"
                      Grid.Row="0"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Stretch="Uniform"
                      MediaOpened="MediaElement_MediaOpened"
                      MediaEnded="MediaElement_MediaEnded"
                      MediaFailed="MediaElement_MediaFailed"
                      ScrubbingEnabled="True"/>

        <Border Grid.Row="1" Background="#AA000000" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="120">
                <TextBlock x:Name="subtitleText"
                           Text="Subtitles will appear here."
                           Foreground="White"
                           FontSize="28"
                           TextAlignment="Center"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- Delay Adjustment Controls -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,5,0,10">
            <Label x:Name="delayLabel"
                   Content="Delay: 0 ms"
                   VerticalAlignment="Center"
                   Foreground="#333333"
                   FontSize="14"/>
            <Button x:Name="decreaseDelayBtn"
                    Content="d-"
                    Margin="5,0"
                    Padding="8,3"
                    Click="DecreaseDelay_Click"/>
            <Button x:Name="increaseDelayBtn"
                    Content="d+"
                    Margin="5,0"
                    Padding="8,3"
                    Click="IncreaseDelay_Click"/>
        </StackPanel>
    </Grid>
</Window>